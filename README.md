# Demo BCC Filkom Multimodal

Fullstack Bun + Hono. Upload KTP atau invoice, model mengembalikan JSON, schema yang memutuskan lolos atau review. Tanpa API key, demo lanjut pakai fallback JSON.

```bash
bun install
cp .env.example .env
bun run dev
```

Buka `http://localhost:3000`.
